---
# Safe default: read-only, and blocks (fails) if drift is found. Every
# playbook in playbooks/ sets network_baseline_stage explicitly - this
# only matters if the role is ever invoked without one.
network_baseline_stage: precheck
